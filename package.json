{
  "name": "shortly",
  "private": true,
  "scripts": {
"dev": "concurrently -n backend,frontend -c blue,green \"go run ./backend\" \"bun run --cwd frontend dev\"",
"dev:backend": "go run ./backend",
"dev:frontend": "bun run --cwd frontend dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
