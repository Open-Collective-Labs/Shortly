{
  "name": "shortly",
  "private": true,
  "scripts": {
"dev": "concurrently -n backend,frontend -c blue,green \"cd backend && go run ./cmd/server\" \"bun run --cwd frontend dev\"",
"dev:backend": "cd backend && go run ./cmd/server",
"dev:frontend": "bun run --cwd frontend dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
